import 'package:flutter/material.dart';

class SightListScreenApp extends StatelessWidget {
  const SightListScreenApp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SightListScreenApp();
  }
}

void main() {
  runApp(const SightListScreenApp());
}
